<h1><%= t ".title" %></h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @document_types.each do |document_type| %>
  <tr>
    <td><%= document_type.name %></td>
    <td><%= link_to t(".show"), document_type %></td>
    <td><%= link_to t(".edit"), edit_document_type_path(document_type) %></td>
    <td><%= link_to t(".destroy"), document_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to t(".new"), new_document_type_path %>
