<h1><%= t ".title" %></h1>

<table>
  <tr>
    <th>Serie</th>
    <th>Consecutive type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @consecutives.each do |consecutive| %>
  <tr>
    <td><%= consecutive.serie %></td>
    <td><%= consecutive.consecutive_type_id %></td>
    <td><%= link_to t(".show"), consecutive %></td>
    <td><%= link_to t(".edit"), edit_consecutive_path(consecutive) %></td>
    <td><%= link_to t(".destroy"), consecutive, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to t(".new"), new_consecutive_path %>
