<h1><%= t ".title" %></h1>

<table>
  <tr>
    <th>Description</th>
    <th>Code status</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @invoice_statuses.each do |invoice_status| %>
  <tr>
    <td><%= invoice_status.description %></td>
    <td><%= invoice_status.code_status %></td>
    <td><%= link_to t(".show"), invoice_status %></td>
    <td><%= link_to t(".edit"), edit_invoice_status_path(invoice_status) %></td>
    <td><%= link_to t(".destroy"), invoice_status, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to t(".new"), new_invoice_status_path %>
