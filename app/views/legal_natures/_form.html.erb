<%= form_for(@legal_nature) do |f| %>
  <% if @legal_nature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@legal_nature.errors.count, "error") %> prohibited this legal_nature from being saved:</h2>

      <ul>
      <% @legal_nature.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
