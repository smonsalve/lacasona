<%= form_for(@business_document_type) do |f| %>
  <% if @business_document_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@business_document_type.errors.count, "error") %> prohibited this business_document_type from being saved:</h2>

      <ul>
      <% @business_document_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
