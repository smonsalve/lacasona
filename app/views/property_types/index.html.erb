<h1><%= t ".title" %></h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @property_types.each do |property_type| %>
  <tr>
    <td><%= property_type.name %></td>
    <td><%= link_to t(".show"), property_type %></td>
    <td><%= link_to t(".edit"), edit_property_type_path(property_type) %></td>
    <td><%= link_to t(".destroy"), property_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to t(".new"), new_property_type_path %>
